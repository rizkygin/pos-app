import { FeaturePicker } from "@/components/order/feature-picker";

export default function OrderPage() {
    // i want in this have all promo like grid of 3 columns,
    // and whenever they click promo it will trigger to outlet who pay to 

    return <FeaturePicker />;
}
